# Track D — Frontend + App — Change Log (review-1 branch)

Owner: Track D (UI, explainability graph, Docker, browser verification)
Branch: `review-1`
Standing rule: Work is in `D:\Capstone\frontend\`. No commits until user says so; restart frontend via subagent when needed. Stay on `review-1` (12d behind main locally before rebase — already handled via review-1 assembly).

## Change Log

### 2026-08-27 — Initial tracking files created
- Created `tracking/` per-track files.

### 2026-08-27 — Hosting confirmed
- Next 16.3.0 + Tailwind v4, `frontend\.env.local` -> `NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000`, runs via `npm run dev` on :3000.

### 2026-08-27 — Fix: explain empty creators (pairs with Track C change)
- `frontend/src/types/index.ts:43` — `BrandRecommendationResponse` now `explanation?: string; counts?: {[k:string]:number}`.
- `frontend/src/app/dashboard/page.tsx:35` — Added empty-results UI: when `result.results.length===0`, show title `"No creators matched \"{product_category}\", budget ₹…"` plus `explanation` paragraph and `counts` breakdown (Considered / dropped_by_*) instead of blank list.

### 2026-08-27 — Fix: demo polish — dashboard/monitoring/explainability (Task 3)
- **Motivation:** Demo showed technical placeholder noise and un-clickable handles; graph text claimed no data despite 340+1414 live edges; LeBron risk was a smoke-test leftover.
- **Files:**
  - `frontend/src/app/dashboard/page.tsx:93` — (1) Handles now clickable `<a>` to Instagram (`https://www.instagram.com/<handle>`) / YouTube (`https://www.youtube.com/@<handle>`) / Reddit (`/r/` or `/user/`); name also links via priority `instagram → youtube → reddit`. (2) Removed placeholder hints below Sentiment/Feature (`"placeholder 0.5 (Temporal 0%)"`) and removed `"raw GAIL output; final_score clamped [0,100]"` sublabel (kept only basis-independent score). (3) Removed `"confidence 0–100"` line and `basis` technical paragraph; confidence now only in explainability's simplified "Estimated range X–Y". Ordering: `SpilloverBadge` now receives `influencerName` for personalized tooltip.
  - `frontend/src/components/SpilloverBadge.tsx:36` — (4) Simplified hover: `TOOLTIP_COPY` → `tooltipCopy(basis, influencerName)` returning human-readable one-liners (`"Estimated for <name> from similar creators they collaborate with — not directly trained, so treat as wider estimate"` etc.) and removed the `sentiment_risk_score is still placeholder …` footnote. `SpilloverBadge` props now `{basis, compact?, influencerName?}`.
  - `frontend/src/app/monitoring/page.tsx:15` — No file change needed for LeBron fix (DB `riskalert` resolved); page already correctly hides resolved alerts (`GET /alerts` filters `resolved=false`). If re-seeding is needed, re-apply `UPDATE riskalert SET resolved=false WHERE id=2`.
  - `frontend/src/app/explainability/page.tsx:1` — (1) Header comment updated: graph edges are live (340 collaborations + 1414 co-occurrences via `GET /feature-store/edges/*`, GAIL `c6488a6`), visualization placeholder remains UI gap, not data gap. (2) Per-creator `"Confidence bounds … (basis: inferred, hw≈5.25 …) is_mock_data …"` replaced with simplified `"Estimated range 0–100 for <name>"`. (3) Removed `"placeholder 0.5 (Temporal 0%)"` hints from Contribution boxes (now just label+points). (4) Mono formula `"99.3 = (0.4×1.73 + …) ×100 + ~0.0"` replaced with human line `"Final score X — Spillover Y pts (40%) + Sentiment Z pts (30%) + Features W pts (30%)"`. (5) Removed `"raw GAIL spillover … outside nominal 0-1"` and basis technical paragraphs (`"Inferred — graph-connected but unlabeled; GAT inductive..."`) — now only the simplified SpilloverBadge tooltip carries the meaning. (6) Creator Features still 15 pts because `creator_feature_score=0.5` placeholder (see `tracking/TASK2_ANALYSIS.md`); view no longer claims otherwise — hint removed, not fabricated.
- **Verify:** `npm run build` passes; `POST /brand-input` → Dashboard shows clickable Instagram links, no placeholder text, hover reads `"Estimated for <name> from similar creators…"`, Monitoring shows `No alerts yet` (LeBron no longer high), Explainability shows simplified scores + `"Graph data is live … but the interactive … is still a UI placeholder"`.

### 2026-08-27 — Fix: full 259-node interactive graph (vis-network) + sponsorship non‑empty clarification
- **Motivation:** Explainability still showed only a footer placeholder paragraph, not the interactive 259‑node graph the user requested with filters + search; sponsorship layer was already non‑empty (16 edges) but not rendered, and the “≤10 recommendation set vs ~54‑pair supervision” confusion needed clarifying.
- **Files:**
  - `frontend/src/types/index.ts:51` — Added `CollaborationEdge {source_creator_id,target_creator_id,weight}` and `SponsorshipEdge {creator_id,brand_id,content_id,platform}`.
  - `frontend/src/lib/api.ts:29` — Added `getCollaborationEdges() → GET /feature-store/edges/collaborations`, `getCoOccurrenceEdges() → GET /feature-store/edges/co-occurrence`, `getSponsorshipEdges() → GET /feature-store/edges/sponsorships`.
  - `frontend/package.json:1` — `npm install vis-network vis-data` (long‑term pick: `vis-network/standalone` canvas‑rendered, `DataSet/DataView` filtering, clustering — durable vs `d3-force`/`react-force-graph-2d`).
  - `frontend/src/components/CollabGraph.tsx:1` — New client component (dynamic `ssr:false`) loading **all 259 creators** (`getCreators` → `CreatorLite {creator_id,name,category}`), 340 collab + 1,414 co‑occurrence + 16 sponsorship edges via the three helpers, rendering via `vis-network/standalone` `DataSet` + forceAtlas2Based physics. Features: (a) **full 259 always loaded**, active recommendation set (whatever `POST /recommendations` last returned — default 10, cap 50; the ~54‑pair GAIL supervision is distinct) is haloed ★ (`borderWidth 4`, `size 16`, `BASIS_COLOR`); (b) **filters** — `basis` (`trained/inferred/isolated/placeholder` + clear), `edge type` (`collaborates` solid violet / `co‑occurs` dashed gray / `sponsorship` gold squares), `category` select (6 `CREATOR_CATEGORIES`), `weight ≥k` slider (1‑3), `search by name` (`Network.focus`); (c) legend + footer counts; all filters are `DataSet` updates so 259‑node stays smooth. Install is `vis-network/standalone` + `vis-data/peer`, physics `stabilization 250`, interaction `hover:true`.
  - `frontend/src/app/explainability/page.tsx:1` — `dynamic(()=>import("@/components/CollabGraph"),{ssr:false})`, header comment updated to *“Graph is now interactive: vis-network over all 259, 340+1,414+16 (sponsorship populated by POST /labeling/run) — the ~54‑pair supervision is distinct from per‑query max_results”*, and footer placeholder `<p>` replaced with `<div><h2>Network graph — all 259 creators</h2><p>Full graph always loaded (259 nodes … 16 sponsorships — sponsorship non‑empty … The active recommendation set (default 10, cap 50) is haloed</p><CollabGraph/></div>`. Graph is always visible even when `!result` (full 259 with no halo), not gated behind brand query.
- **Data note:** `GET /feature-store/edges/sponsorships →16` verified 2026‑08‑27 (sample `bc6bef8a… instagram DZcZ5WJP-_M`), so sponsorship layer now renders as square brand nodes. If after `POST /labeling/run` it were `0`, it would have been an empty‑state banner — but user’s “multiple sponsored creators” recollection was correct, so it renders.
- **Verify:** `npm run build` now passes (fixed `animation: {duration, easingFunction}`), `GET /explainability →200` (27746b after restart), `GET /feature-store/edges/*` re‑verified 259/340/1414/16; `npm run lint` clean.

---

## Master Prompt — Track D (update after each change)

> You are Track D (Frontend+App) on branch `review-1`. Your ownership is `frontend/src/app/` (5 routes), `frontend/src/lib/api.ts:1`, `frontend/src/components/SpilloverBadge.tsx:51`, `frontend/src/types/index.ts:1`, `WIREFRAMES.md:1`, `HANDOFF.md:1`, `frontend/package.json:1`, plus explainability/monitoring pages.
>
> **Current state (2026-08-27, after full‑graph batch):**
> - API surface: `lib/api.ts:15` `POST /recommendations` returns `{query,results,is_mock_data,explanation?,counts?}` + `lib/api.ts:29` `GET /feature-store/edges/collaborations|co‑occurrence|sponsorships` (`getCreators` now used as `CreatorLite {creator_id,name,category}`); `types/index.ts` has `CollaborationEdge` + `SponsorshipEdge`.
> - Dashboard: `app/dashboard/page.tsx:15` handles three states: (a) `!result` → "No recommendation query yet" (b) `results.length===0` → show `explanation` + `counts` (c) results present → ranked list with `SpilloverBadge` (emerald/violet/zinc, personalized `influencerName`), clickable handles (instagram→youtube→reddit), cleaned `ScorePart`s (no placeholder hints, no `"raw GAIL…"` or `"confidence …"`). `is_mock_data` banner retained.
> - SpilloverBadge: `components/SpilloverBadge.tsx:36` now `tooltipCopy(basis,influencerName)` → human‑readable one‑liners.
> - Explainability: `app/explainability/page.tsx:1` now renders `CollabGraph` (dynamic `vis-network/standalone` over **all 259** creators, 340 collab + 1,414 co‑occurrence + 16 sponsorships — sponsorship non‑empty via `POST /labeling/run` disclosure extraction, not an empty banner; the ~54‑pair supervision is counted separately from per‑query `max_results` default 10 cap 50 — active set is haloed ★ inside the full 259). Header reflects `vis-network/standalone` + full counts; per‑creator stays simplified. Filters: `basis` pills, `edge type` (collab/co‑occurrence/sponsorship), `category` select, `weight ≥k` slider, `search by name` (focuses node). Env stays pooler `:8000`.
>
> **Replay instructions (exact diffs to re-apply in order):**
> 1. `frontend/src/types/index.ts:43` — Change `BrandRecommendationResponse` to include `explanation?: string; counts?: {[k:string]: number};`; `frontend/src/types/index.ts:51` — add `CollaborationEdge {source_creator_id,target_creator_id,weight}` and `SponsorshipEdge {creator_id,brand_id,content_id,platform}`.
> 2. `frontend/src/lib/api.ts:29` — Add `getCollaborationEdges() → fetch GET /feature-store/edges/collaborations`, `getCoOccurrenceEdges() → GET /feature-store/edges/co‑occurrence`, `getSponsorshipEdges() → GET /feature-store/edges/sponsorships` (import new types from `@/types`).
> 3. `frontend/package.json:1` — `npm install vis-network vis-data` (long‑term pick: `vis-network/standalone` canvas, `DataSet/DataView` filtering — durable vs `d3-force`/`react-force-graph`).
> 4. `frontend/src/components/CollabGraph.tsx:1` — New client component (dynamic `ssr:false`) via `vis-network/standalone` + `vis-data/peer` `DataSet`, loading all 259 creators (`CreatorLite {creator_id,name,category}` via `getCreators`), 340 collab + 1,414 co‑occurrence + 16 sponsorship edges, rendering full 259 always loaded with filters: `basis` pills (`trained/inferred/isolated/placeholder` + clear), `edge type` toggles (`collab` solid / `co‑occurs` dashed / `sponsorship` gold squares), `category` select, `weight ≥k` slider, `search by name` (focuses `Network.focus`), highlight: active recommendation set (whatever `POST /recommendations` last returned — default 10, cap 50; the ~54‑pair supervision is distinct) is haloed ★ (`BASIS_COLOR`, `borderWidth 4`). Physics `forceAtlas2Based {gravitationalConstant:-45, springLength:85}` + `stabilization 250` + `stabilizationIterationsDone → fit({animation:{duration:600,easingFunction:"easeInOutQuad"}})` + `click → focus({scale:1.1})`. See Change Log 2026‑08‑27 entry 3 for full diff.
> 5. `frontend/src/app/dashboard/page.tsx:35` — After the `if (!result) return (...)` block, insert the `if (result.results.length===0) return (<empty-state with explanation+counts>)` branch per Change Log entry 1.
> 6. `frontend/src/app/dashboard/page.tsx:93` — Replace `result.results.map` body: make `profileUrl` via priority `instagram→youtube→reddit`, render `influencer.name` as `<a href={profileUrl} target="_blank">` when present, render `instagram_handle`/`youtube_handle`/`reddit_handles` as individual `<a>` links (reduce with `" · "`), pass `influencerName` to `<SpilloverBadge basis={basis} influencerName={influencer.name}>`, remove `isOutOfRange` / `"raw GAIL…"` sublabel, remove `"placeholder 0.5"` hints from `ScorePart`, and remove the `confidence …` + `basis:` line (keep only `final_score`). See Change Log 2026‑08‑27 entry 2.
> 7. `frontend/src/components/SpilloverBadge.tsx:36` — Replace `TOOLTIP_COPY: Record<SpilloverBasis,string>` with `function tooltipCopy(basis,influencerName?:string):string` returning human‑readable one‑liners per basis (see Change Log), change props to `{basis, compact?:boolean, influencerName?:string}`, `const copy = tooltipCopy(basis,influencerName)`, and remove the `sentiment_risk_score is still placeholder …` footnote inside the tooltip.
> 8. `frontend/src/app/explainability/page.tsx:1` — Add `dynamic(()=>import("@/components/CollabGraph"),{ssr:false})`, update header comment to vis‑network full‑259 note, replace the `result.results.map` body per Change Log (remove mono formula / `isOutOfRange` / `hw≈5.25` / `is_mock_data` / basis paragraphs, add simplified final‑score line + three `Contribution`s without hints + `"Estimated range X–Y for <name>"`), and replace footer placeholder `<p>` with `<div><h2>Network graph — all 259</h2><p>Full graph always loaded (259 nodes … 16 sponsorships — sponsorship non‑empty … active set (default 10, cap 50) is haloed</p><CollabGraph/></div>` per Change Log entry 3.
> 9. DB: `UPDATE riskalert SET resolved=true WHERE id=2` (LeBron smoke‑test) so `GET /alerts` → `[]`; no file change needed on `monitoring/page.tsx`.
> 10. `backend/app/routers/influencers.py:54` — Add `CATEGORY_RATE` + `_rate_for` and switch both `estimated_cost` sites to `_rate_for(creator.category)` (Track C change but demo‑consistent).
> 11. Restart frontend via subagent/WMI (`npm run dev` from `frontend/`, port 3000 — WMI `vis-network/standalone` is client‑only so SSR not broken) and backend if rate tier changed; verify `npm run build` passes, `GET /explainability →200` shows `CollabGraph` with 259 nodes, filters instant, search focuses, and `GET /feature-store/edges/sponsorships →16` renders brand squares.
